# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-14 14:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/application that implements text-to-image generation pipelines using the FLUX diffusion model family (FLUX.1-schnell, FLUX.1-dev, and FLUX2/Klein variants). It is built on top of the Candle ML framework (Hugging Face's Rust-native tensor/neural network library) and provides the following technical responsibilities: model loading and configuration for multiple FLUX model variants, diffusion scheduling logic, and end-to-end pipeline execution that takes text prompts and produces images. The architecture uses a plugin/registry pattern where different model variants register themselves as Generator trait implementations, allowing the system to select and invoke the appropriate generation backend at runtime. As a single-project repository, all components are self-contained within candle-gen itself. It depends on the Candle ML framework and likely associated Hugging Face ecosystem crates (e.g., for tokenization and model weight loading). No downstream dependents were identified in the analysis, so it likely functions as a standalone tool or library for running FLUX-based image generation locally in Rust.

## Projects

### candle-gen

candle-gen is a Rust project that implements image generation pipelines using the FLUX diffusion model family (including FLUX.1-schnell, FLUX.1-dev, and FLUX2/Klein variants), built on top of the Candle ML framework. It provides model loading, configuration, scheduling, and pipeline execution for text-to-image generation, exposing a plugin/registry system where different model variants register themselves as Generator implementations.

