# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-01 08:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust repository providing generative image model backends built on top of the Candle ML framework. The primary analyzed crate, candle-gen-boogu, implements a diffusion/flow-matching image generation pipeline (referred to as 'Boogu', likely a FLUX-style model variant). It exposes both text-to-image and instruction-based image-editing workflows, handling model configuration, sigma/timestep scheduling, reference image conditioning, and transformer-compatible layer normalization utilities. The repository appears architecturally organized around a common abstraction (a `Generator` trait) that multiple backend crates implement, allowing different generative model implementations to be swapped or combined behind a unified interface. There are no observed cross-repository dependencies, suggesting it is a self-contained library. Its primary external dependency is the Candle ML framework (Hugging Face's Rust-native tensor/neural-network library). Consumers of this repository would likely be Rust applications or services that need to perform local, on-device generative image inference without Python or PyTorch.

## Projects

### candle-gen

candle-gen-boogu is a Rust crate implementing a diffusion-based image generation backend (likely a FLUX/flow-matching model variant called 'Boogu') using the Candle ML framework. It provides text-to-image and image-editing (instruction-based) generation pipelines, including model configuration, sigma/timestep scheduling, reference image conditioning, and a layer-norm utility for transformer-style model loading. It appears to be one of potentially several generator backends in a larger 'candle-gen' repository that abstracts multiple generative model implementations behind a common `Generator` trait.

