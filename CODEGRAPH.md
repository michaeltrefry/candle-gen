# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-20 19:05 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/framework for image generation using diffusion model pipelines, built on top of the candle machine learning framework. It implements noise scheduling (beta and sigma schedules), model loading, and end-to-end image generation pipelines targeting a diffusion model called 'Chroma' with multiple variants (base, flash, HD). The architecture is modular: different model descriptors can be registered and resolved at runtime, allowing configurable diffusion parameters per variant. The repository appears to be a self-contained project with no detected cross-repository dependencies, serving as either a standalone tool or a library that consumers would depend on to perform diffusion-based image synthesis in Rust.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that implements diffusion model pipelines, specifically targeting a model called 'Chroma' with multiple variants (base, flash, HD). It handles noise scheduling (including beta and sigma schedules), model loading, and image generation pipelines using the candle ML framework. The project appears to be a modular generator system where different model descriptors can be registered and resolved, with configurable diffusion parameters.

