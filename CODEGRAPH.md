# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 00:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation backend that implements diffusion model inference pipelines using the Candle ML framework (HuggingFace's Rust tensor library). It supports at least two pipeline variants — 'turbo' (likely Stable Diffusion Turbo or similar fast inference) and 'edit' (instruction-based image editing, possibly InstructPix2Pix or similar). The codebase handles model configuration and loading, timestep scheduling for the diffusion process, and generation request processing including reference image conditioning for edit-mode operations. It registers itself as a 'candle' provider for one or more model IDs, suggesting it is a plugin or backend module within a larger generative AI serving system that supports multiple provider backends. The single project in this repository constitutes the entire codebase — there are no multiple sub-projects to coordinate. External dependencies include the Candle ML framework for tensor operations and model inference. What depends on this repository is unclear from the evidence, but the provider-registration pattern implies a host system that dynamically loads or dispatches to backend implementations like this one.

## Projects

### candle-gen

candle-gen is a Rust-based image generation backend that implements diffusion model pipelines (specifically 'turbo' and 'edit' variants) using the Candle ML framework. It handles model configuration, loading, timestep scheduling, and generation request processing including reference image conditioning for edit operations. The project appears to be a plugin or backend module within a larger generative AI system, registering itself as a 'candle' provider for multiple model IDs.

