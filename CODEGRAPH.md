# candle-gen

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-13 02:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library crate that implements an image generation pipeline using the FLUX diffusion model family, built on top of the candle machine learning framework. It provides two model variants (Schnell and Dev), loads model weights from safetensor files via memory-mapped variable builders, and executes a denoising diffusion loop to produce generated images. The crate exposes a Generator trait/interface along with model descriptors, designed to integrate with an external gen-core registry that can discover and invoke image generation backends by name. The repository consists of a single Rust project with no observed internal sub-crates; its primary dependency is the candle ML framework (for tensor operations and model primitives), and it is intended to be consumed by a higher-level orchestration layer (gen-core) that manages model registration and dispatch.

## Projects

### candle-gen

candle-gen is a Rust project that provides an image generation pipeline built on top of the FLUX diffusion model (via the candle ML framework). It implements two model variants (Schnell and Dev), handles model loading from safetensor files via memory-mapped var builders, runs the denoising diffusion loop, and exposes a Generator interface with model descriptors for registration and resolution by a higher-level gen-core registry.

