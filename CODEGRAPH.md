# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-01 22:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation backend built around a diffusion-model pipeline, likely leveraging the 'candle' ML framework (Hugging Face's Rust ML library). It implements the core infrastructure for text-to-image and image-editing workflows, including: model configuration and loading, sigma/timestep scheduling for diffusion sampling, and multi-modal conditioning that combines text prompts with reference images. The repository defines a generator abstraction (via a gen_core module/crate) that provides a unified interface over these diffusion pipeline components. It appears to be a self-contained backend library or service with no observed cross-repository dependencies, though it likely depends on the candle ML framework crates and potentially HuggingFace model weights. Nothing external has been observed depending on it yet. The single project within the repository handles all pipeline stages internally.

## Projects

### candle-gen

candle-gen is a Rust-based image generation backend that implements a diffusion-model pipeline (likely based on the 'candle' ML framework) supporting text-to-image and image-editing workflows. It handles model configuration, sigma/timestep scheduling, model loading, and multi-modal conditioning (text + reference images) for a generator abstraction defined in gen_core.

