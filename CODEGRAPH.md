# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-01 09:05 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/backend for image generation using diffusion models, built on the Candle ML framework. It implements a flow-matching diffusion pipeline (referred to internally as 'boogu', likely a FLUX variant) that supports two primary capabilities: text-to-image generation and instruction-based image editing. The repository is organized around a plugin/generator trait system that abstracts model loading, noise scheduling (sigma computation), and inference pipeline execution. Key technical responsibilities include: model configuration management, diffusion timestep/sigma scheduling, model weight loading and initialization via Candle, and orchestrating the forward pass through the diffusion network. As a single-project repository, all components are internally coupled — the trait abstractions, model implementations, and pipeline orchestration work together within the same crate. No cross-repository dependencies were identified, meaning it likely functions as a standalone backend service or library. External dependencies center on the Candle ML framework (Hugging Face's Rust tensor/neural-net library) for tensor operations and model execution. The confidence in this characterization is medium because the analysis relies on inferred architecture from code structure rather than comprehensive documentation or runtime behavior observation.

## Projects

### candle-gen

candle-gen is a Rust-based image generation backend implementing a diffusion model pipeline (likely a FLUX/flow-matching variant called 'boogu') using the Candle ML framework. It provides text-to-image and image-editing (instruction-based) generation capabilities, managing model configuration, noise scheduling/sigma computation, model loading, and inference pipelines through a plugin/generator trait system.

